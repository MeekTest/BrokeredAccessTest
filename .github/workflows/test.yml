name: Test

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Load secrets from 1Password
        id: op
        uses: 1Password/load-secrets-action@v5beta
        with:
          export-env: true
        env:
          OP_WORKLOAD_ID: "Y6K5R5TKLJA23NIMI3DKQAHLTE"
          OP_ENVIRONMENT_ID: "t3vb75qwdjlhxpoo3rkkha42te"
          OP_INTEGRATION_KEY: ${{ secrets.OP_INTEGRATION_KEY }}
      
      - name: Verify secret
        run: |
          if [ -z "$TEST" ]; then
            echo "ERROR: Secret is empty"
            exit 1
          else
            echo "Secret loaded successfully (${#TEST} characters)"
          fi
